general:
  seed: null
  checkpoint: null # continue with weights
  freeze_backbone: false # train only last layer

  project_name: mix3d
  workspace: kumuji
  experiment_name: baseline_scannet2000

  save_dir: saved/${general.experiment_name}/${now:%Y-%m-%d_%H%M}
  # time/commit/md5(config)_uuid
  # time/experiment_id/version_uuid
  experiment_id: test_scannet200 # commit[:8], or unique from logger
  version: test_scannet200 # md5[:8] of config

  gpus: 1

defaults:
  - data: indoor
  - data/data_loaders: simple_loader
  - data/datasets: scannet200
  - data/collation_functions: voxelize_collate
  - logging: minimal
  - model: baseline
  - metrics: miou
  - optimizer: adamw
  - scheduler: onecyclelr
  - trainer: trainer
  - callbacks: callbacks
  - loss: cross_entropy

hydra:
  run:
    dir: saved/hydra_logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: saved/hydra_logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
    # dir: ${general.save_dir}
    subdir: ${hydra.job.num}_${hydra.job.id}
